# Support

Use GitHub Issues for reproducible bugs and feature requests.

Before opening an issue:

- update to the latest release;
- run the analyzer with `--format=json` when useful;
- reduce the problem to a minimal example;
- include relevant Laravel/PHP versions and queue `after_commit` configuration;
- remove credentials and customer data.

Commercial Laravel development and consulting: https://www.codegenie.be
